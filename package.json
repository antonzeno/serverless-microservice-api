{
  "name": "serverless-nodejs-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "serverless offline --stage dev",
    "deploy": "serverless deploy --stage prod --region eu-north-1",
    "remove": "serverless remove --stage prod --region eu-north-1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "aws-sdk": "^2.1691.0",
    "drizzle-orm": "^0.33.0",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.2",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^13.8.0"
  }
}
